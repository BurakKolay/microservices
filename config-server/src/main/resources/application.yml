spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        default-label: master
        git:
          uri: https://github.com/BurakKolay/microservice-config-server
  profiles:
    active: dev
server:
  port: 8888
